/**
 * Polydeukes — a development discipline framework for building alongside an AI
 * coding partner.
 *
 * Pre-alpha. This package reserves the unscoped `polydeukes` name and is the
 * future umbrella / `pdks` CLI entry point; it intentionally exports nothing yet.
 * The version lives in package.json only (single source of truth); a future
 * `pdks --version` reads it at the CLI layer, which is allowed to touch files.
 * The covenant, ledger, memory, and verify modules live in their own
 * `@polydeukes/*` packages and are in design.
 * See https://github.com/huskyhoochu/polydeukes
 */

export {};
